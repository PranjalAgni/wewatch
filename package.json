{
  "name": "wewatch",
  "version": "1.0.0",
  "main": "index.js",
  "packageManager": "npm@10.9.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dev:web": "turbo run dev --filter=web",
    "db:migrate": "npm run prisma migrate dev --workspace web",
    "db:push": "npm run prisma db push --workspace web"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "turbo": "^2.5.6"
  }
}
